spring:
  application:
    name: learning-spring-boot

  datasource:
    url: jdbc:postgresql://localhost:5432/springbootDb
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true